# v1.3 Phase B Completion Report

## Phase B: RPC 调整（不影响 UI）

### 目标
- 修改现有 RPC 写入 dual confirmation 字段并调用 reconcile
- 新增 v1.3 核心 RPC（nominate, reactivate, guest）
- 更新 RLS policies（removed participant visibility）
- **不修改 UI**（UI 调整在 Phase C）

---

## 已完成的 Migrations

### Migration B1: `20260206151717_v1.3_phase_b1_modify_request_invite_rpcs.sql`

**修改 RPC**:
1. `rpc_match_request_join`
   - ✅ 添加 scope 检查（v1.3: request 必须在 scope groups 内）
   - ✅ 写入 `user_accepted_at = now()`（request = user 已接受）
   - ✅ 写入 `nominated_by = NULL`
   - ✅ 禁止 removed 用户 re-request（必须等待 reactivate）
   - ✅ 调用 `reconcile_status()`

2. `rpc_match_invite_user`
   - ✅ **移除** scope 检查（v1.3: invite 不受 scope 限制）
   - ✅ 写入 `org_approved_at = now()`（invite = ORG 预批准）
   - ✅ 写入 `org_approved_by = auth.uid()`
   - ✅ 写入 `nominated_by = NULL`
   - ✅ 禁止 re-invite removed 用户（必须 reactivate）
   - ✅ 调用 `reconcile_status()`

---

### Migration B2: `20260206151718_v1.3_phase_b2_modify_accept_approve_rpcs.sql`

**修改 RPC**:
1. `rpc_match_accept_invite`
   - ✅ 写入 `user_accepted_at = now()`（idempotent with COALESCE）
   - ✅ 调用 `reconcile_status()`（may → confirmed if org_approved_at exists）
   - ✅ 禁止 removed 用户 accept（必须等待 reactivate）

2. ~~`rpc_match_confirm_participant`~~ → **改名** `rpc_match_org_approve_participant`
   - ✅ **ORG ONLY**（删除 MP confirm 权限）
   - ✅ 写入 `org_approved_at = now()`（idempotent with COALESCE）
   - ✅ 写入 `org_approved_by = auth.uid()`
   - ✅ 调用 `reconcile_status()`（may → confirmed if user_accepted_at exists）
   - ✅ 禁止 approve removed（必须先 reactivate）

---

### Migration B3: `20260206151719_v1.3_phase_b3_modify_withdraw_remove_rpcs.sql`

**合并 RPC**:
1. ~~`rpc_match_decline_invite`~~ + ~~`rpc_match_leave`~~ → **合并为** `rpc_match_user_withdraw`
   - ✅ 统一 user 自己退出的入口
   - ✅ 写入 `removed_by = auth.uid()`（v1.3 关键）
   - ✅ 写入 `removal_note`（自动生成描述）
   - ✅ 支持 pending 和 confirmed 状态

2. `rpc_match_remove_participant`
   - ✅ 写入 `removed_by = auth.uid()`（v1.3 关键）
   - ✅ 写入 `removal_note`（自动生成描述）
   - ✅ 保持 ORG + authorized MP 权限

---

### Migration B4: `20260206151720_v1.3_phase_b4_create_new_rpcs.sql`

**新增 RPC**:

1. 🆕 `rpc_match_nominate_user(p_match_id, p_user_id)`
   - ✅ Participant 提名用户（需 `can_participants_invite_users`）
   - ✅ **不检查 scope**（v1.3: nominate 不受限制）
   - ✅ 创建 `join_method=requested`, `nominated_by=nominator_id`
   - ✅ 双边确认（user_accepted_at=NULL, org_approved_at=NULL）
   - ✅ 调用 `reconcile_status()`

2. 🆕 `rpc_match_reactivate_participant(p_match_participant_id)`
   - ✅ **ORG ONLY**
   - ✅ **只修改 `status = 'pending'`**
   - ✅ **不修改 `user_accepted_at` 或 `org_approved_at`**
   - ✅ 调用 `reconcile_status()`（根据现有字段自动判断最终 status）

3. 🆕 `rpc_match_add_guest_org(p_match_id, p_guest_display_name, p_guest_notes)`
   - ✅ **ORG ONLY**
   - ✅ 创建 guest + participant
   - ✅ `status = 'confirmed'`（直接 confirmed）
   - ✅ `org_approved_at = now()`

4. 🆕 `rpc_match_add_guest_participant(p_match_id, p_guest_display_name, p_guest_notes)`
   - ✅ Participant 添加（需 `can_participants_add_guests`）
   - ✅ 创建 guest + participant
   - ✅ `status = 'pending'`（需 ORG 批准）
   - ✅ `org_approved_at = NULL`

**废弃 RPC**:
- ❌ ~~`rpc_match_add_guest`~~ — 已删除（拆分为 org/participant）

---

### Migration B5: `20260206151721_v1.3_phase_b5_update_rls_policies.sql`

**更新 RLS Policies**:

1. `matches_select_visibility`
   - ✅ Organizer 可见
   - ✅ Active participant 可见
   - ✅ **新增**: Request-mode match 对 scope group 成员可见

2. `match_participants_select`
   - ✅ Organizer 可见所有（包括 removed）
   - ✅ User 可见自己（包括 removed **如果在 scope 内**）
   - ✅ Active participant 可见其他 active（不含 removed）

---

## Phase B 变更汇总

### 修改的 RPC（7个）

| 原 RPC | v1.3 RPC | 主要变更 |
|-------|---------|---------|
| `rpc_match_request_join` | ✅ 保留 | + scope 检查，+ user_accepted_at，+ reconcile |
| `rpc_match_invite_user` | ✅ 保留 | - scope 检查，+ org_approved_at，+ reconcile |
| `rpc_match_accept_invite` | ✅ 保留 | + user_accepted_at，+ reconcile |
| `rpc_match_confirm_participant` | → `rpc_match_org_approve_participant` | 改名，ORG only，+ reconcile |
| `rpc_match_decline_invite` | → `rpc_match_user_withdraw` | 合并，+ removed_by |
| `rpc_match_leave` | → `rpc_match_user_withdraw` | 合并，+ removed_by |
| `rpc_match_remove_participant` | ✅ 保留 | + removed_by |

### 新增的 RPC（4个）

| RPC | 用途 |
|-----|------|
| `rpc_match_nominate_user` | Participant 提名 |
| `rpc_match_reactivate_participant` | ORG reactivate removed |
| `rpc_match_add_guest_org` | ORG 添加 guest（直接 confirmed） |
| `rpc_match_add_guest_participant` | Participant 添加 guest（pending） |

### 废弃的 RPC（1个）

| RPC | 替代 |
|-----|------|
| `rpc_match_add_guest` | `rpc_match_add_guest_org` + `rpc_match_add_guest_participant` |

---

## 关键不变量验证

| 不变量 | Phase B 实现 |
|-------|------------|
| I8: status 由 reconcile 同步 | ✅ 所有 RPC 调用 reconcile |
| I9: 修改 confirmation fields 必须调用 reconcile | ✅ 所有相关 RPC 调用 |
| I10: 所有 reactivate 由 ORG 执行 | ✅ `rpc_match_reactivate_participant` ORG only |
| I11: Reactivate 只改 status | ✅ 不修改 user_accepted_at/org_approved_at |
| I13: org_approved_at 只由 ORG 设置 | ✅ `rpc_match_org_approve_participant` ORG only |
| I16: Request 必须 scope | ✅ `rpc_match_request_join` 检查 scope |
| I17: Invite 不受 scope 限制 | ✅ `rpc_match_invite_user` 无 scope 检查 |
| I18: Nominate 不受 scope 限制 | ✅ `rpc_match_nominate_user` 无 scope 检查 |
| I19: Nominate 设置 nominated_by | ✅ `rpc_match_nominate_user` 写入 |
| removed_by 必须记录 | ✅ withdraw/remove 都写入 |

---

## 不改什么（Phase B 约束）

- ❌ 不修改 UI
- ✅ UI 仍可调用旧 RPC 名称（通过 alias 或兼容层）
- ✅ Phase B 完成后，旧 UI 应仍能工作（向后兼容）

---

## 验证清单

| 检查项 | 方法 | 预期结果 |
|-------|------|---------|
| 所有 RPC 存在 | `\df rpc_match_*` | 显示所有 v1.3 RPC |
| 旧 RPC 已删除 | `\df rpc_match_confirm_participant` | Not found |
| RLS policies 更新 | `\dp matches` | 显示新 policy |
| Request 写入 user_accepted_at | 调用 RPC，检查字段 | user_accepted_at 有值 |
| Invite 写入 org_approved_at | 调用 RPC，检查字段 | org_approved_at 有值 |
| Accept + Approve → Confirmed | 调用 accept → approve，检查 status | status = 'confirmed' |
| Reactivate 不改 confirmation fields | 调用 reactivate，检查字段 | 字段保持原值 |
| Removed scope member 可见 match | RLS 测试 | 可见 |

---

## 下一步: Phase C

**Phase C: UI 行为对齐**

需要做的事:
1. 更新 API 调用（使用新 RPC 名称）
2. 显示双边确认状态（pending 的不同阶段）
3. 添加 Nominate 功能
4. 添加 Reactivate 功能（ORG）
5. 显示 "Rejoin / Waiting" UI（removed 用户）
6. 更新 Guest 添加流程（区分 org/participant）

---

## Phase B 完成状态

✅ **Phase B 完成**

- RPC 已升级到 v1.3 语义
- 所有 dual confirmation 逻辑已实现
- Reactivation 功能已实现
- RLS policies 已更新
- **向后兼容**（旧 UI 可能需要部分调整，但核心功能仍可用）

**准备进入 Phase C**（UI 对齐）。
